[phases.setup]
aptPkgs = ["python3", "python3-pip", "ffmpeg"]

[phases.install]
cmds = [
  "python3 -m pip install --break-system-packages --no-cache-dir -U -r requirements.txt",
  'python3 -c "import yt_dlp"'
]
